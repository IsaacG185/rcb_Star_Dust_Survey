[Pre-Meeting Notes]
Adjusted 01_Load_Cutout.ipynb:
    - Downloaded 10,629 good plates of R CrB from 15,217. The 4,588 have been saved for
    further analysis/salvaging. This took 11hr 35min.

Adjusted 02_Source_Identification_And_Sorting_By_Algorithm.ipynb:
    - Applied median subtraction to all pixels:
         # Background estimation
        mean, median, std = sigma_clipped_stats(data, sigma=3.0)
        data_sub = data - median # Subtracting the median from the plate
    - Implemented filters on plates that work best:
        - good_match are best plates, these can be saved using plate_db.pkl
    - Adjusted error categorization and identificaiton (i.e., dust, scratches, saturation)
    - Adding (still WIP) selection feature. It is based on filters already developed, and then
    allows user to input stars. Then it runs the algorithms again on all plates, using the
    paradigm plate as reference.
    - Implemented APASS B Magnitudes : Look at question below\
    - Installed and did initial tests on DeepDisc (not PyTorch)

Questions
    - Future concern: It appears that, around R CrB, there are not many stars to test it upon.
    I can do a subtraction (i.e. dim star vs. R CrB), but a linearity check does not display enough
    statistics. Any ideas?
    - Need to change time when we are meeting. When works best?


Meeting Notes:

- More cutouts from same plate might be necessary
- sort by plate limit as well: 17-16; when selecting PP, use plate limit, above 9th ish is saturated
- Lightcurve saturation; get GTIs
- Plate measurements: some stars are variable, we want to keep static stars, for the most part, apart from R CrB. Look up the stars in the cutout.
- Subtract stars on both ends of linearity check (differential photometry), then compare it to what APASS says for B magnitudes/instrumental magntiudes. Can calculate using photgraphic magnitudes,
which is essentially instrumentational magnitudes (it is a parameter of the emulsion that has been calculated before).
- Not many of the known RCB stars are going to be in the plates becuase they are very faint. ZTF could be useful for modern CCD data Northern stars.